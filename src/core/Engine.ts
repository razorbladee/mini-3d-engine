import {Scene} from './Scene'; import {Camera} from '../cameras/Camera'; import {PerspectiveCamera} from '../cameras/PerspectiveCamera'; import {WebGLRenderer} from '../rendering/WebGLRenderer'; export class Engine {scene=new Scene(); camera:Camera; renderer:WebGLRenderer; private frame=0; private last=0; constructor(options:{canvas:HTMLCanvasElement;camera?:Camera}){this.renderer=new WebGLRenderer(options.canvas);this.camera=options.camera||new PerspectiveCamera(60,1);this.resize();window.addEventListener('resize',()=>this.resize())} resize(){const w=this.renderer.canvas.clientWidth||innerWidth,h=this.renderer.canvas.clientHeight||innerHeight;this.renderer.setSize(w,h);if(this.camera instanceof PerspectiveCamera){this.camera.aspect=w/Math.max(h,1);this.camera.updateProjectionMatrix()}} start(update?:(time:{deltaTime:number;elapsed:number})=>void){this.last=performance.now();const tick=(now:number)=>{const dt=Math.min((now-this.last)/1000,0.1);this.last=now;update?.({deltaTime:dt,elapsed:now/1000});this.renderer.render(this.scene,this.camera);this.frame=requestAnimationFrame(tick)};this.frame=requestAnimationFrame(tick)} stop(){cancelAnimationFrame(this.frame)} dispose(){this.stop();this.renderer.dispose()} }

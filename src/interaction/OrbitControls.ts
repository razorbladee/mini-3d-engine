import {Camera} from '../cameras/Camera'; export class OrbitControls {private dragging=false;private x=0; constructor(public camera:Camera,public element:HTMLElement){element.addEventListener('pointerdown',e=>{this.dragging=true;this.x=e.clientX});element.addEventListener('pointerup',()=>this.dragging=false);element.addEventListener('pointermove',e=>{if(!this.dragging)return;camera.position.x+=(e.clientX-this.x)*0.01;this.x=e.clientX})} dispose(){this.element.replaceWith(this.element.cloneNode(true))} }

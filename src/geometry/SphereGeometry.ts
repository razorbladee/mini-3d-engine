import {BufferGeometry} from './BufferGeometry'; export class SphereGeometry extends BufferGeometry { constructor(radius=1,segments=16,rings=8){const p:number[]=[];for(let y=0;y<rings;y++){const v0=y/rings,v1=(y+1)/rings,t0=v0*Math.PI,t1=v1*Math.PI;for(let x=0;x<segments;x++){const u0=x/segments,u1=(x+1)/segments;const q=(u:number,t:number)=>[radius*Math.sin(t)*Math.cos(u*2*Math.PI),radius*Math.cos(t),radius*Math.sin(t)*Math.sin(u*2*Math.PI)];const a=q(u0,t0),b=q(u1,t0),c=q(u1,t1),d=q(u0,t1);p.push(...a,...b,...c,...a,...c,...d)}}super(p)} }

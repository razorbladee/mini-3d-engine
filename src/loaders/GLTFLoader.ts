import { Node } from '../core/Node'; import { Mesh } from '../objects/Mesh'; import { BufferGeometry } from '../geometry/BufferGeometry'; import { StandardMaterial } from '../materials/StandardMaterial';
export type LoadedModel={scene:Node;animations:unknown[]};
export class GLTFLoader {
  async load(url:string):Promise<LoadedModel>{const response=await fetch(url);if(!response.ok)throw new Error(`Unable to load glTF: ${response.status} ${response.statusText}`);const base=new URL('.',url);return url.toLowerCase().endsWith('.glb')?this.parseGlb(await response.arrayBuffer(),base):this.parseJson(await response.json(),base)}
  async parseGlb(data:ArrayBuffer,base=new URL('.',globalThis.location?.href??'http://localhost/')):Promise<LoadedModel>{const view=new DataView(data);if(view.getUint32(0,true)!==0x46546c67)throw new Error('Invalid GLB header');const jsonLength=view.getUint32(12,true);const jsonType=view.getUint32(16,true);if(jsonType!==0x4e4f534a)throw new Error('GLB JSON chunk is missing');const json=JSON.parse(new TextDecoder().decode(new Uint8Array(data,20,jsonLength)));const binHeader=20+jsonLength;const binLength=binHeader+8<=data.byteLength?view.getUint32(binHeader,true):0;const binType=binHeader+8<=data.byteLength?view.getUint32(binHeader+4,true):0;const buffers=binType===0x004e4942?[data.slice(binHeader+8,binHeader+8+binLength)]:[];return this.build(json,buffers,base)}
  async parseJson(json:any,base:URL):Promise<LoadedModel>{const buffers=await Promise.all((json.buffers??[]).map(async(b:any)=>{if(b.uri?.startsWith('data:'))return this.decodeDataUri(b.uri);const r=await fetch(new URL(b.uri,base));return r.arrayBuffer()}));return this.build(json,buffers,base)}
  private decodeDataUri(uri:string){const raw=atob(uri.split(',')[1]);const bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);return bytes.buffer}
  private build(json:any,buffers:ArrayBuffer[],_base:URL):LoadedModel{const root=new Node();const access=(index:number)=>{const a=json.accessors[index],b=json.bufferViews[a.bufferView],buffer=buffers[b.buffer];const component=a.componentType===5126?Float32Array:a.componentType===5123?Uint16Array:Uint8Array;const count=a.type==='VEC3'?3:a.type==='VEC2'?2:1;return new component(buffer,(b.byteOffset??0)+(a.byteOffset??0),a.count*count)};const meshes=(json.meshes??[]).map((mesh:any)=>{const primitive=mesh.primitives?.[0];if(!primitive?.attributes?.POSITION)return null;const positions=Array.from(access(primitive.attributes.POSITION)) as number[];const normals=primitive.attributes.NORMAL?Array.from(access(primitive.attributes.NORMAL)) as number[]:undefined;const uvs=primitive.attributes.TEXCOORD_0?Array.from(access(primitive.attributes.TEXCOORD_0)) as number[]:undefined;const pbr=json.materials?.[primitive.material]?.pbrMetallicRoughness;return new Mesh(new BufferGeometry(positions,normals,uvs),new StandardMaterial({color:'#b5b1c6',roughness:pbr?.roughnessFactor??.5,metalness:pbr?.metallicFactor??0}))});const scene=json.scenes?.[json.scene??0];for(const nodeIndex of scene?.nodes??[]){const node=json.nodes?.[nodeIndex];const mesh=meshes[node?.mesh];if(mesh){if(node.translation)mesh.position.set(...node.translation);root.add(mesh)}}return{scene:root,animations:json.animations??[]}}
}

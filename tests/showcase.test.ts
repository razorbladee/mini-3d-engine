import { describe, expect, it } from 'vitest'; import { examples, nextExample } from '../examples/showcase-registry';
describe('example registry',()=>{it('includes texture and model scenes',()=>{expect(examples).toHaveLength(15);expect(examples.filter(e=>e.group==='Models')).toHaveLength(3);expect(examples.filter(e=>e.group==='Textures')).toHaveLength(4)});it('cycles examples',()=>{expect(nextExample('primitives')).toBe('materials');expect(nextExample('postprocess')).toBe('primitives')})});

import { describe, expect, it } from 'vitest'; import { CapsuleGeometry, CylinderGeometry, TorusGeometry } from '../src';
describe('primitive regressions', () => { it('closes the cylinder with caps', () => { const geometry = new CylinderGeometry(1, 2, 12); expect(geometry.positions.length / 3).toBe(12 * 12); expect(geometry.normals.some((value) => value === 1)).toBe(true); expect(geometry.normals.some((value) => value === -1)).toBe(true); }); it('generates non-empty torus and capsule surfaces', () => { for (const geometry of [new TorusGeometry(), new CapsuleGeometry()]) { expect(geometry.positions.length).toBeGreaterThan(0); expect(geometry.normals.length).toBe(geometry.positions.length); expect(geometry.boundingRadius).toBeGreaterThan(0); } }); });
import {describe,expect,it} from 'vitest';import {CapsuleGeometry,CylinderGeometry,TorusGeometry} from '../src';
describe('primitive regressions',()=>{it('closes the cylinder with caps',()=>{const g=new CylinderGeometry(1,2,12);expect(g.positions.length/3).toBe(12*12);expect(g.normals.some(v=>v===1)).toBe(true);expect(g.normals.some(v=>v===-1)).toBe(true)});it('generates non-empty torus and capsule surfaces',()=>{for(const g of [new TorusGeometry(),new CapsuleGeometry()]){expect(g.positions.length).toBeGreaterThan(0);expect(g.normals.length).toBe(g.positions.length);expect(g.boundingRadius).toBeGreaterThan(0)}})});

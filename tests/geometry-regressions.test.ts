import{describe,expect,it}from'vitest';import{CapsuleGeometry,CylinderGeometry,TorusGeometry}from'../src';
describe('primitive regressions',()=>{it('creates closed cylinder topology',()=>{const g=new CylinderGeometry(1,2,12);expect(g.positions.length/3).toBe(12*12);expect(g.normals.some(v=>v===1)).toBe(true);expect(g.normals.some(v=>v===-1)).toBe(true)});it('creates valid torus and capsule surfaces',()=>{for(const g of[new TorusGeometry(),new CapsuleGeometry()]){expect(g.positions.length).toBeGreaterThan(0);expect(g.normals.length).toBe(g.positions.length);expect(g.boundingRadius).toBeGreaterThan(0)}});it('has no degenerate triangles',()=>{for(const g of[new CylinderGeometry(),new TorusGeometry(),new CapsuleGeometry()])for(let i=0;i<g.positions.length;i+=9){const ax=g.positions[i+3]-g.positions[i],ay=g.positions[i+4]-g.positions[i+1],az=g.positions[i+5]-g.positions[i+2],bx=g.positions[i+6]-g.positions[i],by=g.positions[i+7]-g.positions[i+1],bz=g.positions[i+8]-g.positions[i+2];expect(Math.hypot(ay*bz-az*by,az*bx-ax*bz,ax*by-ay*bx)).toBeGreaterThan(1e-7)}})});

import{describe,expect,it}from'vitest';import{GLTFLoader}from'../src';
describe('GLTFLoader',()=>{it('rejects invalid GLB headers',async()=>{await expect(new GLTFLoader().parseGlb(new ArrayBuffer(8))).rejects.toThrow('Invalid GLB header')});it('rejects empty scenes',async()=>{const json={scenes:[{nodes:[0]}],nodes:[{}],meshes:[],buffers:[]};await expect(new GLTFLoader().parseJson(json,new URL('https://example.com/'))).rejects.toThrow('no renderable meshes')});it('discovers a mesh node not referenced by the active scene',async()=>{const bytes=new ArrayBuffer(36);const view=new DataView(bytes);for(let i=0;i<9;i++)view.setFloat32(i*4,[0,0,0,1,0,0,0,1,0][i],true);const json={buffers:[{uri:'data:application/octet-stream;base64,'+btoa(String.fromCharCode(...new Uint8Array(bytes)))}],bufferViews:[{buffer:0,byteOffset:0,byteLength:36}],accessors:[{bufferView:0,componentType:5126,count:3,type:'VEC3'}],meshes:[{primitives:[{attributes:{POSITION:0}}]}],nodes:[{mesh:0}],scenes:[{nodes:[]}],scene:0};await expect(new GLTFLoader().parseJson(json,new URL('https://example.com/'))).resolves.toMatchObject({scene:expect.anything()})})});

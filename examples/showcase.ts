import { AmbientLight, BasicMaterial, BoxGeometry, DirectionalLight, Engine, Mesh, Node, OrbitControls, OrthographicCamera, PerspectiveCamera, PlaneGeometry, PointLight, PostProcess, Raycaster, SimplePhysics, SphereGeometry, StandardMaterial, Texture2D, Vector3 } from '../src';
import { nextIndex, pointerToNdc } from './showcase-utils';
import './showcase.css';

const root = document.querySelector<HTMLDivElement>('#showcase');
if (!root) throw new Error('The examples page requires a #showcase root');
root.innerHTML = `<header class="topbar"><a class="brand" href="#top"><b>◈</b><span>mini / field notes</span></a><nav><a href="#geometry">3D</a><a href="#interaction">Interaction</a><a href="#extensions">Extensions</a></nav><a class="repo" href="https://github.com/razorbladee/mini-3d-engine" target="_blank" rel="noreferrer">Source ↗</a></header><main id="top"><section class="hero"><p class="eyebrow">MINI 3D ENGINE · LIT REFERENCE</p><h1>The engine,<br><em>with depth.</em></h1><p>Actual WebGL scenes with normals, depth testing, ambient light, directional light, point light, cameras, raycasting, physics, textures, and post-processing.</p><div class="capability-line"><span>WebGL2</span><span>Normals</span><span>Phong lighting</span><span>Perspective + ortho</span><span>Raycasting</span><span>Physics</span></div></section><section class="chapter" id="geometry"><div class="chapter-heading"><p>01 / RENDERING</p><h2>Volume, not stickers.</h2><span>Every solid below uses geometry normals and the lit StandardMaterial path.</span></div><article class="feature feature-wide"><div class="copy"><span class="index">A</span><h3>Primitive parade</h3><p>Box, sphere, and plane, each with a different light response. The sphere uses radial normals, the box uses face normals, and the plane is double-sided.</p><div class="legend"><i class="violet"></i>box <i class="orange"></i>sphere <i class="green"></i>plane</div></div><div class="viewport"><canvas id="geometry-canvas"></canvas><span class="hud">3 meshes · 3 lights · depth on</span></div></article><div class="split"><article class="feature"><div class="copy"><span class="index">B</span><h3>Scene graph</h3><p>A lit parent carries three children. Rotate the parent and watch inherited world matrices keep every volume together.</p></div><div class="viewport compact"><canvas id="graph-canvas"></canvas><span class="hud">TRS + normals + culling</span></div></article><article class="feature"><div class="copy"><span class="index">C</span><h3>Two projections</h3><p>Switch between perspective depth and orthographic projection without changing the scene.</p><button class="action" id="camera-toggle">Use orthographic <span>⇄</span></button></div><div class="viewport compact"><canvas id="camera-canvas"></canvas><span class="hud" id="camera-hud">PerspectiveCamera · fov 58°</span></div></article></div></section><section class="chapter contrast" id="interaction"><div class="chapter-heading"><p>02 / INTERACTION</p><h2>Touch the scene.</h2><span>Lighting stays on while interaction changes the camera and selection.</span></div><article class="feature feature-wide"><div class="copy"><span class="index">D</span><h3>Raycast + orbit</h3><p>Click a lit mesh to select it. Drag horizontally to move the camera using OrbitControls.</p><p class="readout" id="pick-result">Waiting for a hit</p></div><div class="viewport dark"><canvas id="interaction-canvas"></canvas><span class="hud">click to select · drag to orbit</span></div></article><article class="feature feature-wide physics-row"><div class="copy"><span class="index">E</span><h3>Gravity, no drama</h3><p>Three lit bodies fall through the same physics step and settle on the floor.</p><button class="action light" id="drop-again">Drop again <span>↓</span></button><p class="readout" id="physics-readout">Simulating 3 bodies</p></div><div class="viewport dark"><canvas id="physics-canvas"></canvas><span class="hud">gravity -9.81 · floor y=0</span></div></article></section><section class="chapter" id="extensions"><div class="chapter-heading"><p>03 / EXTENSIONS</p><h2>Small APIs, visible proof.</h2><span>Texture decoding, material metadata, light nodes, and ordered passes.</span></div><div class="extension-grid"><article class="extension texture-demo"><span class="index">F</span><h3>Texture2D</h3><p>Generate, encode, and decode a checker in the browser.</p><canvas id="texture-canvas"></canvas><button class="action" id="load-texture">Decode texture <span>↗</span></button><p class="readout" id="texture-result">Ready</p></article><article class="extension material-demo"><span class="index">G</span><h3>Materials + lights</h3><p>StandardMaterial carries roughness and metalness; the renderer collects three light node types.</p><div class="material-orbit"><i></i><b></b><em></em></div><dl><div><dt>roughness</dt><dd>0.24</dd></div><div><dt>metalness</dt><dd>0.78</dd></div><div><dt>lights</dt><dd id="light-count">3</dd></div></dl></article><article class="extension post-demo" id="post-demo"><span class="index">H</span><h3>PostProcess</h3><p>Registered passes execute in order and change the preview.</p><div class="post-art"><i></i><b></b><em></em></div><button class="action" id="post-toggle">Run 2 passes <span>◐</span></button><p class="readout" id="post-result">Passes armed</p></article></div></section></main><footer><span>mini / 0.1</span><span>Every 3D canvas above is running repository code.</span><a href="#top">Back to top ↑</a></footer>`;

const engines: Engine[] = [];
function engine(canvasId: string, camera: PerspectiveCamera | OrthographicCamera = new PerspectiveCamera(58, 1, 0.1, 100)) { const canvas = document.querySelector<HTMLCanvasElement>(`#${canvasId}`); if (!canvas) throw new Error(`Missing example canvas: ${canvasId}`); const value = new Engine({ canvas, camera }); engines.push(value); return value; }
function studio(scene: Node) { scene.add(new AmbientLight('#fff7ed', 0.32)); const sun = new DirectionalLight('#fff5dc', 1.45); sun.direction.set(-0.5, -0.8, -0.35); scene.add(sun); const fill = new PointLight('#8068dc', 3); fill.position.set(-3, 2, -3); fill.distance = 12; scene.add(fill); }
function lit(color: string, roughness = 0.36, metalness = 0.08) { return new StandardMaterial({ color, roughness, metalness }); }

const geometryEngine = engine('geometry-canvas'); studio(geometryEngine.scene);
const geometryMeshes = [new Mesh(new BoxGeometry(1.5), lit('#8068dc', 0.28, 0.18)), new Mesh(new SphereGeometry(0.95, 24, 16), lit('#df8f56', 0.2, 0.45)), new Mesh(new PlaneGeometry(1.8, 1.8), lit('#3fa783', 0.6, 0.02))];
geometryMeshes[0].position.set(-2.1, 0, -6); geometryMeshes[1].position.set(0, 0, -6); geometryMeshes[2].position.set(2.1, 0, -6); geometryMeshes[2].rotation.x = -0.35; geometryEngine.scene.add(...geometryMeshes);
geometryEngine.start(({ elapsed }) => { geometryMeshes[0].rotation.set(elapsed * 0.55, elapsed * 0.8, 0); geometryMeshes[1].rotation.y = elapsed * 0.45; geometryMeshes[2].rotation.z = elapsed * 0.35; });

const graphEngine = engine('graph-canvas'); studio(graphEngine.scene); const pivot = new Node(); pivot.position.z = -6; for (let index = 0; index < 3; index += 1) { const child = new Mesh(new BoxGeometry(0.72), lit(['#8068dc', '#df8f56', '#3fa783'][index], 0.25, 0.25)); const angle = index * Math.PI * 2 / 3; child.position.set(Math.cos(angle) * 1.65, Math.sin(angle) * 1.1, 0); child.scale.set(1, 1 + index * 0.25, 1); pivot.add(child); } graphEngine.scene.add(pivot); graphEngine.start(({ elapsed }) => { pivot.rotation.z = elapsed * 0.38; pivot.rotation.y = elapsed * 0.25; });

const perspective = new PerspectiveCamera(58, 1, 0.1, 100); const orthographic = new OrthographicCamera(-3.2, 3.2, 2.2, -2.2, 0.1, 100); const cameraEngine = engine('camera-canvas', perspective); studio(cameraEngine.scene); const cameraGroup = new Node(); cameraGroup.position.z = -7; for (let index = 0; index < 5; index += 1) { const mesh = new Mesh(new SphereGeometry(0.48 + index * 0.08, 18, 12), lit(index % 2 ? '#df8f56' : '#8068dc', 0.22, 0.4)); mesh.position.set((index - 2) * 1.05, Math.sin(index * 1.7) * 0.55, index * 0.55); cameraGroup.add(mesh); } cameraEngine.scene.add(cameraGroup); cameraEngine.start(({ elapsed }) => { cameraGroup.rotation.y = Math.sin(elapsed * 0.35) * 0.35; }); let orthographicActive = false; document.querySelector<HTMLButtonElement>('#camera-toggle')!.onclick = (event) => { orthographicActive = !orthographicActive; cameraEngine.camera = orthographicActive ? orthographic : perspective; (event.currentTarget as HTMLButtonElement).firstChild!.textContent = orthographicActive ? 'Use perspective ' : 'Use orthographic '; document.querySelector('#camera-hud')!.textContent = orthographicActive ? 'OrthographicCamera · width 6.4' : 'PerspectiveCamera · fov 58°'; };

const interactionCamera = new PerspectiveCamera(58, 1, 0.1, 100); const interactionEngine = engine('interaction-canvas', interactionCamera); studio(interactionEngine.scene); const pickMeshes: Mesh[] = []; for (let index = 0; index < 5; index += 1) { const mesh = new Mesh(index % 2 ? new SphereGeometry(0.68, 20, 12) : new BoxGeometry(1.1), lit(index % 2 ? '#df8f56' : '#8068dc', 0.25, 0.3)); mesh.name = `object-${index + 1}`; mesh.position.set((index - 2) * 1.35, index % 2 ? 0.45 : -0.5, -6 - Math.abs(index - 2) * 0.35); pickMeshes.push(mesh); } interactionEngine.scene.add(...pickMeshes); const interactionCanvas = interactionEngine.renderer.canvas; new OrbitControls(interactionCamera, interactionCanvas); const raycaster = new Raycaster(); interactionCanvas.addEventListener('click', (event) => { const hit = raycaster.setFromCamera(pointerToNdc(event.clientX, event.clientY, interactionCanvas.getBoundingClientRect()), interactionCamera).intersectObjects(pickMeshes)[0]; for (const mesh of pickMeshes) mesh.scale.set(1, 1, 1); if (hit) { hit.object.scale.set(1.28, 1.28, 1.28); document.querySelector('#pick-result')!.textContent = `${hit.object.name} · distance ${hit.distance.toFixed(2)}`; } else document.querySelector('#pick-result')!.textContent = 'Miss · try the center of a mesh'; }); interactionEngine.start(({ elapsed }) => pickMeshes.forEach((mesh, index) => { mesh.rotation.y = elapsed * (0.22 + index * 0.04); }));

const physicsEngine = engine('physics-canvas'); studio(physicsEngine.scene); const physics = new SimplePhysics(); const bodies: Mesh[] = []; const floor = new Mesh(new BoxGeometry(1), lit('#34323f', 0.5, 0.05)); floor.position.set(0, -0.62, -7); floor.scale.set(6, 0.12, 1.1); physicsEngine.scene.add(floor); function resetBodies() { for (const body of bodies) { physics.removeBody(body); physicsEngine.scene.remove(body); } bodies.length = 0; for (let index = 0; index < 3; index += 1) { const body = new Mesh(index === 1 ? new SphereGeometry(0.58, 20, 12) : new BoxGeometry(0.9), lit(['#8068dc', '#df8f56', '#3fa783'][index], 0.3, 0.2)); body.position.set((index - 1) * 1.45, 2.2 + index * 1.15, -7); physics.addBody(body, new Vector3(index === 0 ? 0.12 : index === 2 ? -0.12 : 0, 0, 0)); physicsEngine.scene.add(body); bodies.push(body); } } resetBodies(); document.querySelector<HTMLButtonElement>('#drop-again')!.onclick = resetBodies; physicsEngine.start(({ deltaTime }) => { physics.step(deltaTime); bodies.forEach((body, index) => { body.rotation.z += deltaTime * (index + 1) * 0.45; }); document.querySelector('#physics-readout')!.textContent = bodies.some((body) => body.position.y > 0) ? 'Bodies airborne' : 'All bodies settled'; });

const textureCanvas = document.querySelector<HTMLCanvasElement>('#texture-canvas')!; const textureContext = textureCanvas.getContext('2d'); if (!textureContext) throw new Error('Texture example requires a 2D canvas'); textureCanvas.width = 480; textureCanvas.height = 320; for (let y = 0; y < 8; y += 1) for (let x = 0; x < 12; x += 1) { textureContext.fillStyle = (x + y) % 2 ? '#ece8fb' : '#8068dc'; textureContext.fillRect(x * 40, y * 40, 40, 40); } textureContext.fillStyle = '#272632'; textureContext.font = '600 30px system-ui'; textureContext.fillText('Texture2D', 24, 292); document.querySelector<HTMLButtonElement>('#load-texture')!.onclick = async () => { const result = document.querySelector('#texture-result')!; result.textContent = 'Decoding…'; try { const texture = await Texture2D.load(textureCanvas.toDataURL()); result.textContent = `Decoded ${texture.image.width} × ${texture.image.height}`; } catch (error) { result.textContent = error instanceof Error ? error.message : 'Texture decode failed'; } };

const materialScene = new Node(); const standard = new StandardMaterial({ color: '#8068dc', roughness: 0.24, metalness: 0.78 }); void standard; materialScene.add(new AmbientLight('#fff7ed', 0.35), new DirectionalLight('#ffffff', 1.2), new PointLight('#8068dc', 2)); let lightCount = 0; materialScene.traverse((node) => { if (node instanceof AmbientLight || node instanceof DirectionalLight || node instanceof PointLight) lightCount += 1; }); document.querySelector('#light-count')!.textContent = String(lightCount);
const post = new PostProcess(); const applied: string[] = []; post.add({ apply: () => applied.push('contrast') }).add({ apply: () => applied.push('grade') }); let postState = 0; const postLooks = ['graded', 'mono', '']; document.querySelector<HTMLButtonElement>('#post-toggle')!.onclick = () => { applied.length = 0; post.render({} as WebGLTexture, {} as WebGLFramebuffer, {} as WebGL2RenderingContext); postState = nextIndex(postState, postLooks.length); const demo = document.querySelector('#post-demo')!; demo.classList.remove('graded', 'mono'); if (postLooks[postState]) demo.classList.add(postLooks[postState]); document.querySelector('#post-result')!.textContent = `${applied.join(' → ')} · look ${postState + 1}/3`; };
window.addEventListener('load', () => engines.forEach((value) => value.resize()), { once: true });
